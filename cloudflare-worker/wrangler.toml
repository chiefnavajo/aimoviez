# Cloudflare Worker — Edge Rate Limiting for aimoviez
# Deploy: cd cloudflare-worker && npx wrangler deploy

name = "aimoviez-rate-limiter"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# KV namespace for rate limit counters
# Create with: npx wrangler kv:namespace create RATE_LIMITS
# Then replace the id below with your actual namespace ID
[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "d08a583ec7c14f27907474884a7ba894"

# Optional: preview namespace for local development
# Create with: npx wrangler kv:namespace create RATE_LIMITS --preview
# [[kv_namespaces]]
# binding = "RATE_LIMITS"
# id = "YOUR_PREVIEW_KV_NAMESPACE_ID"
# preview_id = "YOUR_PREVIEW_KV_NAMESPACE_ID"
